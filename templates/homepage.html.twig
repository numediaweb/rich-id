{% extends 'base.html.twig' %}

{# Navbar color #}
{% set navbarColor = 'light' %}

{# Page content #}
{% block page_content %}
    <!-- Header -->
    <header id="home" class="header text-white h-fullscreen"
            style="background-image: url({{ asset('build/images/bg_landing.png') }})"
            data-overlay="1">
        <div class="container">
            <div class="row align-items-center h-100">

                <div class="col-md-5">
                    <h1 class="display-3">Intergalactic Taxi Services</h1>
                    <p class="lead-4">RICH ID made space exploration much easier</p>
                    <p class="lead-2 fw-400 mb-7">Easily book your next ride to where you can imagine. Use our dashboard
                        to check available space drivers or invite the ones you know.</p>
                    <a class="btn btn-xl btn-round btn-info" href="{{ path('admin') }}">Go to the admin</a>
                </div>

            </div>
        </div>
    </header><!-- /.header -->
{% endblock %}
